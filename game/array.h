#ifndef ARRAY_H
#define ARRAY_H

template<typename T>
class level{

	private:
		int sizeArray;

	public:
		level();
		level(vector<string>, vector<string>, int); //fix this function
		~level();


};


#endif // !ARRAY_H
